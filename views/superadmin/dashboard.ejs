<%- include('../partials/header') %>
<h3>Super Admin Dashboard</h3>
<div class="row g-3 mt-2">
  <% Object.entries(stats).forEach(([key,value]) => { %>
    <div class="col-md-2"><div class="card shadow-sm"><div class="card-body"><small class="text-muted text-uppercase"><%= key %></small><h2><%= value %></h2></div></div></div>
  <% }) %>
</div>
<%- include('../partials/footer') %>
